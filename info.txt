portriet - 378 x 567
lanscape -738 x 390

GCP Shell Command to build docker or project ------------------>
gcloud builds submit --tag us-central1-docker.pkg.dev/cutcompress/cutcompress-repo/cutcompress-image:latest .

if successfuly created Dockerfile then -->
gcloud run deploy cutcompress-service \
    --image=us-central1-docker.pkg.dev/cutcompress/cutcompress-repo/cutcompress-image:latest \
    --platform=managed \
    --region=us-central1 \
    --allow-unauthenticated

update time ------------>

git pull origin main  # Pull latest changes

gcloud builds submit --tag us-central1-docker.pkg.dev/cutcompress/cutcompress-repo/cutcompress-image:latest .

gcloud run deploy cutcompress-service \
    --image=us-central1-docker.pkg.dev/cutcompress/cutcompress-repo/cutcompress-image:latest \
    --region=us-central1



way 2 ------------------------------------------------------------
===================================================================
It looks like there's an issue with how comments are formatted in your Dockerfile. The error suggests that Docker is interpreting the comment (# Cloud Run requires port 8080) as part of the ENV command, which isn't valid.

Fix:
Remove the inline comment or move it to a separate line above the ENV statement.

Corrected Dockerfile:

# Use an official lightweight Python image
FROM python:3.11-slim

# Set environment variables
ENV PYTHONUNBUFFERED=1
ENV PORT=8080

# Set working directory inside the container
WORKDIR /app

# Install system dependencies for image processing
RUN apt-get update && apt-get install -y --no-install-recommends \
    libheif-examples \
    ghostscript \
    imagemagick \
    libraw-dev \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements.txt first to cache dependencies layer
COPY requirements.txt .

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the entire project into the container
COPY . .

# Expose the correct port
EXPOSE 8080

# Command to run the app with Gunicorn
CMD ["gunicorn", "-w", "4", "-k", "gthread", "--threads", "8", "-b", "0.0.0.0:8080", "main:app"]


Try rebuilding the image:
docker build -t test-image .



next cmd: build docker image
gcloud builds submit --tag gcr.io/$(gcloud config get-value project)/image-studio

Deploy cmd:
gcloud run deploy image-studio \
  --image gcr.io/$(gcloud config get-value project)/image-studio \
  --platform managed \
  --region us-central1 \
  --allow-unauthenticated


