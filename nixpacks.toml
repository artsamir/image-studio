[phases.setup]
aptPkgs = ["libheif-examples", "ghostscript", "imagemagick", "libraw-dev"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn -w 2 -k gthread -b 0.0.0.0:$PORT main:app"